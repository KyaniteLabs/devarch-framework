name: Blacksmith Probe

on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - '.github/workflows/blacksmith-probe.yml'

permissions:
  contents: read

jobs:
  probe:
    uses: KyaniteLabs/.github/.github/workflows/blacksmith-probe.yml@fdec80f61da82dc1e44387a50e30a92170d3d4d2
    with:
      probe_label: complementary-runner-probe
